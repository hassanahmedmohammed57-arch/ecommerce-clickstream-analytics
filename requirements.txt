boto3>=1.34
pandas>=2.0
pyspark>=3.5   # for local dev / testing only (EMR provides its own)

# For the live demo dashboard (recommended)
streamlit>=1.35
altair>=5.0   # used by streamlit charts (optional but nice)
